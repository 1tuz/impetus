# Task 09 Audit Results (Partial)

## ✅ Already Implemented

### 1. Capability Model (types.rs)
```rust
pub enum WebCapability {
    Search,
    Read,
    PrivateRead,
    Download,
    Browser,
    Submit,
    Upload,
}

impl WebCapability {
    pub fn is_read_only(self) -> bool {
        matches!(self, Self::Search | Self::Read | Self::PrivateRead)
    }
}
```
**Status:** ✅ Complete — все требуемые capabilities определены

### 2. Citation & Provenance (types.rs)
```rust
pub struct CitationSource {
    pub citation_id: String,
    pub url: String,
    pub title: Option<String>,
    pub artifact: Option<DurableArtifactRef>,
}

pub struct SearchHit {
    pub citation_id: String,
    // ...
}

pub struct FetchedPage {
    pub citation: CitationSource,
    pub redirect_chain: Vec<String>,
    // ...
}
```
**Status:** ✅ Complete — citations tracked per source

### 3. Artifacts (service.rs)
```rust
pub struct ArtifactPolicy {
    pub persist_threshold_bytes: usize,  // default: 64KB
    pub persist_html_always: bool,
    pub persist_binary_always: bool,
}
```
**Status:** ✅ Complete — large pages persisted

### 4. Backend Fallback (service.rs)
```rust
pub struct WebResearchEngine {
    default_backend: Arc<dyn SearchBackend>,
    builtin_backends: BTreeMap<String, Arc<dyn SearchBackend>>,
    builtin_fallback_order: Vec<String>,
    external_backends: BTreeMap<String, Arc<dyn SearchBackend>>,
    external_fallback_order: Vec<String>,
}
```
**Status:** ✅ Complete — fallback chain exists

### 5. Limits & Bounds (types.rs)
```rust
pub const MAX_SEARCH_RESULTS: usize = 20;
pub const MAX_FETCH_BYTES: usize = 5 * 1024 * 1024;
pub const MAX_FETCH_CHARS: usize = 200_000;
```
**Status:** ✅ Complete — hard limits defined

## ❓ Need Verification (Files Not Fully Read)

### 6. Bounded Multi-Step Loop
**Location:** `service.rs` (rest of file not read)
**Need:** Method like `research_with_depth()` or loop with dedup

### 7. Doctor Backend Health
**Location:** `doctor.rs` (9KB, not read)
**Need:** Per-backend health check, degraded mode detection

### 8. Policy Integration
**Location:** Look for `Policy` usage of `WebCapability`
**Need:** Verify capability checks before actions

### 9. SSRF Tests
**Location:** `tests/` or `crates/*/tests/`
**Need:** Verify redirect/DNS rebinding/private target tests exist

## Next Steps

1. Read full `service.rs` — check for research loop
2. Read `doctor.rs` — verify backend health diagnostics
3. Find and read security tests
4. Search for Policy integration with WebCapability
5. Implement gaps if found
6. Add tests if missing
