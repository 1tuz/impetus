# Next work

The canonical product plan is [docs/ROADMAP.md](docs/ROADMAP.md). This file is
only a short near-term queue; it does not redefine architecture or completion.

1. Complete existing attachment/diff/detail DTO backing services with bounded,
   redacted durable storage—or expose the capability as unavailable.
2. Build the first autonomous Agent Loop and Tool Orchestrator slice through
   the existing safety/effect boundary.
3. Add durable budgets and the rules-based Model Router foundation.
4. Define the standalone `impetus` CLI/TUI slice and the minimal Zap
   discovery/connect/authorize/backend handoff independently.

Do not treat copied forks as a Session DAG, provider registry foundations as a
completed router, or remote stubs as an end-to-end remote agent flow.

